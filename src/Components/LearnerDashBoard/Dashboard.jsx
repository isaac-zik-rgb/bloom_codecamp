import React, {useState} from "react";
import { useNavigate } from "react-router-dom";

const Dashboard = () => {
    return (
        <div className="container">
        <h1>Just the dashboard</h1>
        </div>
    )
};

export default Dashboard;